name: keep-render-awake

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Roomly health endpoint
        run: curl https://roomly-bcjb.onrender.com/roomly/api/v1/health
