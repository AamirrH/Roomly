name: Keep Render Backend Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Ping Roomly backend
        run: |
          curl -fsS "https://roomly-bcjb.onrender.com/roomly/api/v1/health"
